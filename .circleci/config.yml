version: 2
jobs:
  build:
    steps:
      - checkout
      - run:
          name: update-npm
          command: 'sudo npm install -g npm@5'
      - run:
          name: build
          command: npm run build
      - run:
          name: test
          command: npm test